# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local dev artifact written by the /api/progress dev middleware — never ship it
progress.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Lab build scratch — artifacts themselves (public/labs/*.zip) ARE committed,
# because the deploy host has no compilers.
labs/src/**/.work/
labs/src/**/bin/
labs/src/**/obj/
